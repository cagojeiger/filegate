# 설정은 configs/filegate.yaml에 있다. 이 파일은 로드할 config 경로와 개별 오버라이드만.
FILEGATE_CONFIG=configs/filegate.yaml

# 개별 값 오버라이드 예 (FILEGATE__<섹션>__<키>):
# FILEGATE__DATABASE__MAX_CONNECTIONS=10
# FILEGATE__SERVER__LOG_FORMAT=json
