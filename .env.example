# 설정은 쉼표로 구분한 파일들을 병합해 읽는다: 본 설정 + 벤더 자격증명(ESO Secret 대상).
FILEGATE_CONFIG=configs/filegate.yaml,configs/providers.yaml

# 개별 값 오버라이드 예 (FILEGATE__<섹션>__<키>):
# FILEGATE__DATABASE__MAX_CONNECTIONS=10
# FILEGATE__SERVER__LOG_FORMAT=json
